# MongoDB for local development
spring.mongodb.uri=mongodb://127.0.0.1:27017/kavyaprodb_dev
spring.mongodb.database=kavyaprodb_dev
spring.data.mongodb.auto-index-creation=false

# Run server on different port when using dev if desired
server.port=8080

